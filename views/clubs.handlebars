<div class="container" id="all-clubs">
    <h2>Available Clubs:</h2>

    <div class="row">
        {{#each clubs}}
        <div class="col-lg-3 col-md-6 mb-4">
            <div class="card h-100">
                <img class="card-img-top" src={{this.dataValues.club_image_url}} alt="placeholder">
                <div class="card-body">
                    <h4 class="card-title"><a href="/clubs/{{this.dataValues.id}}">{{this.dataValues.club_name}}</a>
                    </h4>
                    <p class="card-text">{{this.dataValues.club_description}}</p>
                </div>
                <div class="card-footer">
                    <a href="#" class="btn btn-primary" data-id={{this.dataValues.id}}>Gravitate!</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

</div>
